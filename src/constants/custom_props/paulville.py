class PaulvilleProp:
    """
    Paulville (prefix RACETRACK3) ships no custom props — it only has its own
    city geometry + textures. Registered for custom city-mesh texture resolution;
    intentionally empty.
    """
